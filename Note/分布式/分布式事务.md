# 分布式事务精讲笔记

> 课程链接：https://www.mashibing.com/study?courseNo=305&sectionNo=58946&courseVersionId=1250
>
> 记录时间：2026-05-13
>
> 课程共 12 节，主题围绕分布式事务的概念、典型场景、XA/2PC/3PC、TCC、本地事件表、最大努力通知等解决方案展开。

## 1. 课程目录记录

| 序号 | 视频标题 | 时长 | 主题定位 |
| --- | --- | ---: | --- |
| 1 | 课程内容介绍 | 2分11秒 | 说明本课程要解决的分布式事务问题与方案全景 |
| 2 | 分布式事务应用场景 | 4分57秒 | 从跨服务、跨库、跨组件的数据一致性场景切入 |
| 3 | 事务的概念 | 3分7秒 | 回顾本地事务、ACID、提交与回滚 |
| 4 | 分布式事务概念 | 2分46秒 | 定义分布式事务，说明它和本地事务的差异 |
| 5 | 分布式事务思路分析 | 5分46秒 | 分析强一致、最终一致、补偿、消息化等思路 |
| 6 | XA规范 | 3分31秒 | 介绍 XA 中的事务管理器、资源管理器和标准接口 |
| 7 | 2pc提交协议 | 10分25秒 | 重点讲两阶段提交的执行流程、成功和失败路径 |
| 8 | 2pc缺点的解决 | 5分34秒 | 分析 2PC 的阻塞、单点、数据不一致等问题与改进 |
| 9 | 3pc提交协议 | 8分53秒 | 介绍三阶段提交如何降低阻塞风险 |
| 10 | tcc解决方案 | 6分33秒 | 讲 Try、Confirm、Cancel 的业务补偿型事务 |
| 11 | 事件表解决方案 | 3分20秒 | 讲本地事件表/消息表如何保证最终一致性 |
| 12 | 最大努力通知方案 | 3分34秒 | 讲通知型最终一致性方案和适用边界 |

## 2. 课程总目标

第 1 节视频中展示的主题是“分布式事务全套解决方案”，重点问题包括：

1. 分布式场景中的数据一致性问题如何解决。
2. 分布式事务问题的回答思路，在面试中如何展开。
3. 定时任务下一次隔夜提交失败时，三阶段提交如何处理。
4. 组合存储组件，比如 Redis、MySQL，如何解决数据一致性。
5. 什么是 TCC，也就是 Try、Confirm、Cancel 解决方案。
6. 如何利用本地事件表实现分布式事务。
7. 最大努力通知方案是什么。

可以把整套课程理解成一条主线：

```text
本地事务
  -> 分布式事务问题
  -> 强一致性方案：XA、2PC、3PC
  -> 柔性事务方案：TCC、本地事件表、最大努力通知
  -> 根据业务一致性要求做方案选型
```

## 3. 为什么会有分布式事务

### 3.1 本地事务的边界

本地事务通常只覆盖一个数据库连接、一个资源管理器或一个单体应用内部的资源操作。例如：

```text
开启事务
  扣减账户 A 余额
  增加账户 B 余额
提交事务
```

如果两个操作都在同一个数据库中，数据库可以通过 redo log、undo log、锁、MVCC 等机制保证一致性。

问题出现在系统拆分后：

```text
订单服务 -> 订单库
库存服务 -> 库存库
积分服务 -> 积分库
支付服务 -> 第三方支付平台
```

一次业务操作要跨多个服务、多个数据库、多个中间件，单个数据库事务无法覆盖全部操作。这时就出现了分布式事务。

### 3.2 典型不一致场景

#### 下单扣库存

```text
1. 订单服务创建订单成功。
2. 库存服务扣减库存失败。
3. 结果：有订单，但库存没有扣减。
```

#### 支付成功改订单状态

```text
1. 支付平台扣款成功。
2. 回调订单服务时网络异常。
3. 结果：用户已经付款，但订单仍是未支付。
```

#### 订单成功发积分

```text
1. 订单服务提交成功。
2. 积分服务调用超时。
3. 结果：订单存在，但积分没有发放。
```

#### MySQL 和 Redis 双写

```text
1. 更新 MySQL 成功。
2. 删除或更新 Redis 缓存失败。
3. 结果：数据库是新值，缓存还是旧值。
```

#### 定时任务跨天处理

```text
1. 业务在当天完成第一阶段操作。
2. 第二阶段由隔夜定时任务继续执行。
3. 如果定时任务失败或重复执行，数据状态可能卡在中间态。
```

这些场景的共同点是：业务动作被拆成多个本地事务，任何一步成功、失败、超时、重试都可能导致整体状态不一致。

## 4. 事务基础

### 4.1 ACID

| 特性 | 含义 | 关键点 |
| --- | --- | --- |
| Atomicity 原子性 | 要么全部成功，要么全部失败 | 依赖 undo log、回滚机制 |
| Consistency 一致性 | 事务前后数据满足业务约束 | 例如余额不能凭空增加 |
| Isolation 隔离性 | 并发事务之间互不干扰 | 依赖锁、MVCC、隔离级别 |
| Durability 持久性 | 提交后的结果不会丢失 | 依赖 redo log、刷盘机制 |

分布式事务最难的是一致性和原子性。因为不同资源各自提交，天然没有一个统一的提交点。

### 4.2 本地事务和分布式事务的差异

| 对比项 | 本地事务 | 分布式事务 |
| --- | --- | --- |
| 涉及资源 | 单库、单连接、单资源 | 多服务、多库、多资源 |
| 协调者 | 数据库自身 | 事务协调器或业务协调逻辑 |
| 一致性 | 强一致更容易 | 往往要在强一致和可用性之间取舍 |
| 失败处理 | 数据库自动回滚 | 需要重试、补偿、对账、人工介入 |
| 性能 | 较高 | 协议型方案性能较低，柔性事务更高 |
| 复杂度 | 较低 | 明显升高，需要处理网络不确定性 |

## 5. 分布式事务的核心困难

### 5.1 网络不可靠

本地方法调用失败通常可以认为操作失败，但远程调用失败不代表对方没有执行。

```text
调用库存服务扣减库存
  -> 请求超时
  -> 可能是库存服务没收到
  -> 可能是库存服务收到了、执行成功了，但响应丢了
  -> 可能是库存服务执行中宕机
```

所以分布式事务不能只用“调用返回值”判断最终结果。

### 5.2 部分成功

一笔业务包含多个参与者时，最常见的问题就是部分成功：

```text
订单成功，库存失败。
库存成功，积分失败。
支付成功，订单失败。
消息发送成功，本地事务失败。
本地事务成功，消息发送失败。
```

### 5.3 状态不可见

协调方无法直接知道每个参与方内部事务的真实状态，只能通过协议、日志、回查接口、幂等接口来推断和修复。

### 5.4 重试带来的重复执行

为了保证最终成功，系统通常会重试。但重试会带来重复扣款、重复发券、重复加积分等风险。因此分布式事务必须配套幂等设计。

## 6. 分布式事务的总体思路

### 6.1 强一致性思路

强一致性方案希望所有参与者像一个整体一样提交或回滚，典型代表是 XA、2PC、3PC。

优点：

- 对业务方更接近本地事务语义。
- 能提供更强的数据一致性。

缺点：

- 性能较差。
- 锁持有时间长。
- 对数据库、中间件、驱动有要求。
- 在网络异常下容易阻塞。

适合：

- 金融核心账务。
- 低并发、高一致性场景。
- 参与资源数量少且可控的场景。

### 6.2 最终一致性思路

最终一致性方案不要求所有系统在同一时刻一致，而是通过异步消息、重试、补偿、对账等机制，保证一段时间后达到一致。

典型代表：

- TCC。
- 本地事件表。
- 可靠消息最终一致性。
- 最大努力通知。
- Saga。

优点：

- 可用性更高。
- 性能更好。
- 更适合互联网高并发业务。

缺点：

- 业务侵入性更强。
- 需要设计中间状态。
- 必须处理幂等、空回滚、悬挂、补偿失败等问题。

适合：

- 电商下单。
- 支付回调。
- 物流通知。
- 积分、优惠券、库存等可补偿业务。

## 7. XA 规范

### 7.1 XA 是什么

XA 是 X/Open 组织定义的分布式事务处理规范，它定义了事务管理器和资源管理器之间的接口。

核心角色：

| 角色 | 英文 | 职责 |
| --- | --- | --- |
| 应用程序 | AP, Application Program | 发起业务操作 |
| 事务管理器 | TM, Transaction Manager | 协调全局事务提交或回滚 |
| 资源管理器 | RM, Resource Manager | 管理具体资源，比如数据库 |

### 7.2 XA 的基本思想

XA 把一次全局事务拆成多个分支事务，每个分支事务由一个资源管理器处理。事务管理器负责协调这些分支：

```text
应用程序
  -> 事务管理器创建全局事务 XID
  -> 数据库 A 执行分支事务 XA-1
  -> 数据库 B 执行分支事务 XA-2
  -> 事务管理器统一决定提交或回滚
```

### 7.3 XA 和 2PC 的关系

XA 是接口规范，2PC 是提交协议。

可以理解为：

```text
XA 规定“事务管理器如何和资源管理器说话”
2PC 规定“事务管理器分几步让大家提交”
```

XA 常用 2PC 作为提交协议。

## 8. 2PC 两阶段提交

### 8.1 核心思想

2PC 全称 Two Phase Commit，即两阶段提交。它把提交过程分成两个阶段：

1. Prepare 阶段：询问所有参与者能否提交。
2. Commit/Rollback 阶段：根据第一阶段结果统一提交或回滚。

### 8.2 角色

| 角色 | 职责 |
| --- | --- |
| 协调者 Coordinator | 发起全局事务，收集投票，决定提交或回滚 |
| 参与者 Participant | 执行本地事务，返回是否可以提交，执行最终指令 |

### 8.3 正常提交流程

```text
阶段一：Prepare

协调者 -> 参与者 A：能否提交？
协调者 -> 参与者 B：能否提交？

参与者 A：
  执行业务 SQL
  写 undo/redo 日志
  锁定资源
  返回 Yes

参与者 B：
  执行业务 SQL
  写 undo/redo 日志
  锁定资源
  返回 Yes

阶段二：Commit

协调者收到所有 Yes
协调者 -> 参与者 A：Commit
协调者 -> 参与者 B：Commit

参与者 A 提交本地事务并释放锁
参与者 B 提交本地事务并释放锁
```

### 8.4 回滚流程

只要有一个参与者在 Prepare 阶段返回 No，协调者就会发送 Rollback：

```text
协调者 -> 参与者 A：Prepare
协调者 -> 参与者 B：Prepare

参与者 A 返回 Yes
参与者 B 返回 No

协调者 -> 参与者 A：Rollback
协调者 -> 参与者 B：Rollback
```

### 8.5 2PC 的关键点

#### 第一阶段不是简单检查

参与者在 Prepare 阶段通常已经执行了本地事务的主要操作，只是还没有最终提交。它需要保证后续可以提交，也可以回滚。

#### Prepare 后会持有锁

参与者一旦返回 Yes，就意味着资源处于等待全局决议的状态。此时相关行锁、事务上下文可能不能释放。

#### 协调者必须可靠记录决议

协调者在决定 Commit 或 Rollback 后，需要把全局事务决议写入日志。否则宕机恢复后无法判断应该继续提交还是回滚。

### 8.6 2PC 的优点

- 思路清晰，语义接近本地事务。
- 能保证较强的一致性。
- 对业务代码侵入较低，更多依赖事务框架、数据库驱动和协调器。

### 8.7 2PC 的缺点

#### 同步阻塞

参与者在 Prepare 后必须等待协调者的最终指令。期间资源被锁住，其他事务可能被阻塞。

#### 协调者单点问题

如果协调者宕机，参与者可能一直不知道该提交还是回滚。

#### 数据不一致风险

第二阶段如果协调者向一部分参与者发送 Commit 成功，向另一部分发送失败，可能出现一部分提交、一部分未提交的状态。

```text
协调者决定 Commit
  -> A 收到 Commit 并提交
  -> B 没收到 Commit
  -> 协调者宕机
结果：A 已提交，B 卡住或未提交
```

#### 性能问题

2PC 需要多轮网络通信，且锁持有时间长，不适合高并发长事务。

## 9. 2PC 缺点的解决思路

2PC 的问题不能完全消除，只能通过工程手段降低影响。

### 9.1 协调者高可用

通过主备、集群、持久化日志、选主机制减少协调者单点故障。

```text
协调者写事务日志
  -> 主节点宕机
  -> 备节点读取日志
  -> 继续发送 Commit/Rollback
```

### 9.2 超时机制

参与者等待协调者指令时不能无限等待，需要设置超时策略。

注意：超时不能随便回滚。因为协调者可能已经决定提交，只是消息延迟。如果参与者自行回滚，而其他参与者提交，就会造成不一致。

### 9.3 事务状态查询

参与者在不确定状态下可以向协调者或其他参与者查询全局事务状态。

```text
参与者 B 未收到 Commit
  -> 查询协调者全局事务状态
  -> 如果状态为 COMMIT，继续提交
  -> 如果状态为 ROLLBACK，执行回滚
```

### 9.4 缩短事务范围

减少 2PC 中参与者数量，缩短 Prepare 到 Commit 的时间，避免把远程调用、慢查询、复杂计算放在全局事务中。

### 9.5 优先使用柔性事务

如果业务可以接受最终一致，就不要强行使用 2PC。很多互联网业务更适合通过消息、补偿和对账解决。

## 10. 3PC 三阶段提交

### 10.1 为什么有 3PC

3PC 全称 Three Phase Commit，是对 2PC 的改进。它试图通过增加一个预提交阶段，减少阻塞问题。

2PC 的最大问题是参与者在 Prepare 后不知道协调者最终决定，如果协调者宕机，参与者容易长时间阻塞。

3PC 把“询问能否提交”和“准备真正提交”拆开：

1. CanCommit：询问是否可以执行事务。
2. PreCommit：预提交，参与者执行事务并准备提交。
3. DoCommit：正式提交。

### 10.2 3PC 流程

#### 第一阶段：CanCommit

```text
协调者 -> 参与者：能不能提交？
参与者检查自身状态、资源、权限
参与者返回 Yes 或 No
```

这个阶段通常不锁定关键资源，主要做可行性判断。

#### 第二阶段：PreCommit

如果所有参与者返回 Yes：

```text
协调者 -> 参与者：PreCommit
参与者执行业务操作
参与者写事务日志
参与者返回 Ack
```

如果有参与者返回 No：

```text
协调者 -> 所有参与者：Abort
```

#### 第三阶段：DoCommit

如果协调者收到所有 Ack：

```text
协调者 -> 参与者：DoCommit
参与者正式提交
参与者释放资源
```

如果出现异常：

```text
协调者 -> 参与者：Abort
参与者回滚
```

### 10.3 3PC 的超时机制

3PC 在协调者和参与者侧都引入超时机制：

- 协调者等待参与者响应超时，可以发送 Abort。
- 参与者等待协调者指令超时，可以根据当前阶段自行决策。

典型规则：

```text
如果处于 CanCommit 阶段超时：倾向于 Abort。
如果处于 PreCommit 阶段且已经收到 PreCommit：超时后倾向于 Commit。
```

原因是：参与者收到 PreCommit 通常说明所有参与者都通过了 CanCommit，协调者大概率会提交。

### 10.4 3PC 对 2PC 的改进

| 问题 | 2PC | 3PC |
| --- | --- | --- |
| 阻塞 | Prepare 后可能长期阻塞 | 引入超时和 PreCommit，降低阻塞 |
| 阶段 | Prepare、Commit/Rollback | CanCommit、PreCommit、DoCommit |
| 参与者自主决策 | 弱 | 更强 |
| 一致性风险 | 存在 | 仍然存在 |

### 10.5 3PC 的局限

3PC 不是银弹。它仍然无法彻底解决网络分区带来的不一致。

例如：

```text
协调者发送 PreCommit 后网络分区。
部分参与者收到 PreCommit。
部分参与者没有收到。
收到 PreCommit 的参与者超时后提交。
没收到 PreCommit 的参与者超时后回滚。
结果仍可能不一致。
```

所以 3PC 更多是理论和协议层面的改进，真实互联网业务中并不如 TCC、可靠消息、本地事件表常见。

## 11. TCC 解决方案

### 11.1 TCC 是什么

TCC 是 Try、Confirm、Cancel 三个阶段的缩写。它是一种业务层面的补偿型分布式事务。

| 阶段 | 含义 | 典型动作 |
| --- | --- | --- |
| Try | 尝试执行业务，完成资源检查和预留 | 冻结库存、冻结余额、创建预占记录 |
| Confirm | 确认执行业务，真正完成资源使用 | 扣减冻结库存、确认扣款 |
| Cancel | 取消执行业务，释放 Try 预留资源 | 解冻库存、解冻余额 |

### 11.2 TCC 和 2PC 的区别

| 对比项 | 2PC/XA | TCC |
| --- | --- | --- |
| 实现层面 | 数据库资源层 | 业务层 |
| 一致性 | 强一致倾向 | 最终一致倾向 |
| 锁粒度 | 数据库锁 | 业务资源预留 |
| 性能 | 较低 | 较高 |
| 业务侵入 | 较低 | 较高 |
| 失败处理 | 协议回滚 | 业务补偿 |

TCC 的本质是把数据库锁变成业务冻结，把数据库回滚变成业务 Cancel。

### 11.3 TCC 示例：下单扣库存

#### Try 阶段

```text
订单服务：
  创建订单，状态为 TRYING 或 PENDING。

库存服务：
  检查库存是否充足。
  将可售库存减少。
  将冻结库存增加。
```

库存表可以设计为：

```sql
CREATE TABLE inventory (
  product_id BIGINT PRIMARY KEY,
  available_count INT NOT NULL,
  frozen_count INT NOT NULL
);
```

Try 扣减逻辑：

```sql
UPDATE inventory
SET available_count = available_count - 1,
    frozen_count = frozen_count + 1
WHERE product_id = ?
  AND available_count >= 1;
```

#### Confirm 阶段

```text
订单服务：
  将订单状态改为 CONFIRMED。

库存服务：
  将冻结库存真正扣掉。
```

```sql
UPDATE inventory
SET frozen_count = frozen_count - 1
WHERE product_id = ?
  AND frozen_count >= 1;
```

#### Cancel 阶段

```text
订单服务：
  将订单状态改为 CANCELED。

库存服务：
  释放冻结库存。
```

```sql
UPDATE inventory
SET available_count = available_count + 1,
    frozen_count = frozen_count - 1
WHERE product_id = ?
  AND frozen_count >= 1;
```

### 11.4 TCC 的关键问题

#### 幂等

Confirm 或 Cancel 可能被重复调用，必须保证多次执行结果一致。

常见做法：

- 每个全局事务使用唯一 `transaction_id`。
- 每个分支事务记录执行状态。
- Confirm/Cancel 前先查状态。
- 已成功则直接返回成功。

分支事务表示例：

```sql
CREATE TABLE tcc_branch_transaction (
  id BIGINT PRIMARY KEY,
  transaction_id VARCHAR(64) NOT NULL,
  branch_name VARCHAR(64) NOT NULL,
  status VARCHAR(32) NOT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  UNIQUE KEY uk_tx_branch (transaction_id, branch_name)
);
```

状态流转：

```text
TRYING -> CONFIRMED
TRYING -> CANCELED
CONFIRMED -> CONFIRMED
CANCELED -> CANCELED
```

#### 空回滚

空回滚指 Try 没有执行成功，但 Cancel 被调用了。

原因可能是：

```text
协调器调用 Try 超时。
参与者实际没有执行 Try。
协调器认为失败，发起 Cancel。
参与者收到 Cancel。
```

解决：

- Cancel 先检查 Try 记录是否存在。
- 如果 Try 记录不存在，插入一条空回滚记录。
- 后续如果 Try 请求迟到，看到已有 Cancel 记录，应拒绝执行，避免悬挂。

#### 悬挂

悬挂指 Cancel 先于 Try 执行，随后迟到的 Try 又执行成功，导致资源被预留但没有机会释放。

```text
Try 请求网络延迟。
协调器超时后发 Cancel。
Cancel 先到达并执行空回滚。
迟到的 Try 又到达。
如果 Try 继续冻结库存，就出现悬挂。
```

解决：

- Try 执行前检查是否已有 Cancel 记录。
- 如果已有 Cancel，Try 直接拒绝。

#### 防并发

同一个事务分支可能同时收到多次 Confirm 或 Cancel，需要用唯一索引、状态机、乐观锁或行锁保证状态流转正确。

### 11.5 TCC 的优点

- 性能好，不需要长时间持有数据库锁。
- 业务语义清晰，适合资源预留类场景。
- 可根据业务设计灵活补偿。

### 11.6 TCC 的缺点

- 业务侵入性强。
- 每个参与方都要实现 Try、Confirm、Cancel。
- 需要处理幂等、空回滚、悬挂、重试、超时。
- 对业务建模要求高，不适合所有操作。

### 11.7 TCC 适用场景

适合：

- 库存冻结。
- 余额冻结。
- 优惠券锁定。
- 机票、酒店、座位预占。
- 订单交易链路。

不适合：

- 无法预留资源的操作。
- 补偿逻辑很难写的操作。
- 参与方不可控的第三方系统。

## 12. 本地事件表解决方案

### 12.1 思想

本地事件表也常被称为本地消息表、Outbox Pattern。核心思想是：

```text
业务数据变更和事件记录写入同一个本地事务。
事务提交后，由后台任务或消息发布器把事件投递到 MQ。
消费者消费消息并执行自己的本地事务。
通过重试和幂等保证最终一致。
```

它解决的是“本地事务成功但消息发送失败”的问题。

### 12.2 为什么不能直接先写库再发消息

错误做法：

```text
1. 订单服务创建订单并提交事务。
2. 订单服务发送扣库存消息。
```

问题：

```text
订单提交成功后，服务宕机。
消息没有发出去。
库存永远不会扣减。
```

反过来先发消息也有问题：

```text
1. 先发送扣库存消息。
2. 再创建订单。
```

如果消息发送成功但订单创建失败，库存服务可能扣了库存，但订单不存在。

### 12.3 本地事件表流程

```text
订单服务本地事务：
  1. 创建订单。
  2. 插入事件表：ORDER_CREATED。
  3. 提交事务。

消息发布器：
  1. 扫描未发送事件。
  2. 发送到 MQ。
  3. 收到 MQ ACK 后，把事件状态改为 SENT。

库存服务消费者：
  1. 消费 ORDER_CREATED 消息。
  2. 幂等检查。
  3. 扣减库存。
  4. 记录消费成功。
```

### 12.4 表结构示例

```sql
CREATE TABLE local_event (
  id BIGINT PRIMARY KEY,
  event_id VARCHAR(64) NOT NULL,
  event_type VARCHAR(64) NOT NULL,
  aggregate_id VARCHAR(64) NOT NULL,
  payload TEXT NOT NULL,
  status VARCHAR(32) NOT NULL,
  retry_count INT NOT NULL DEFAULT 0,
  next_retry_time DATETIME,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  UNIQUE KEY uk_event_id (event_id),
  KEY idx_status_retry (status, next_retry_time)
);
```

消费者幂等表：

```sql
CREATE TABLE consumed_message (
  id BIGINT PRIMARY KEY,
  message_id VARCHAR(64) NOT NULL,
  consumer_group VARCHAR(64) NOT NULL,
  created_at DATETIME NOT NULL,
  UNIQUE KEY uk_msg_consumer (message_id, consumer_group)
);
```

### 12.5 事件状态机

```text
NEW -> SENDING -> SENT
NEW -> SENDING -> FAILED -> SENDING -> SENT
FAILED -> DEAD
```

状态说明：

| 状态 | 含义 |
| --- | --- |
| NEW | 本地事务已提交，事件待发送 |
| SENDING | 正在发送，防止多线程重复发送 |
| SENT | MQ 已确认收到 |
| FAILED | 发送失败，等待重试 |
| DEAD | 超过最大重试次数，需要人工处理 |

### 12.6 关键设计点

#### 业务数据和事件表必须在同一个数据库事务中

这是本方案成立的基础。否则仍然会出现业务成功但事件丢失的问题。

#### 消息发送至少一次

消息发布器可能重复发送同一事件，因此消费者必须幂等。

#### 消费者本地事务

消费者处理消息时，业务操作和消费记录最好放在同一个本地事务中：

```text
开启事务
  插入 consumed_message
  执行业务处理
提交事务
```

如果插入消费记录时发现唯一索引冲突，说明消息已经处理过，直接返回成功。

#### 重试和死信

对于发送失败或消费失败的消息，需要有重试策略：

```text
第 1 次失败：10 秒后重试
第 2 次失败：1 分钟后重试
第 3 次失败：5 分钟后重试
多次失败：进入死信或人工处理
```

### 12.7 优点

- 不依赖 XA，性能好。
- 适合高并发互联网业务。
- 业务数据和事件记录强一致。
- 可以通过 MQ 解耦服务。

### 12.8 缺点

- 需要额外事件表和扫描任务。
- 消费端必须幂等。
- 存在短暂不一致窗口。
- 需要运维监控积压、失败、死信事件。

### 12.9 适用场景

- 订单创建后扣库存。
- 下单后发积分。
- 支付成功后通知多个内部系统。
- 用户注册后发欢迎消息、初始化配置。
- 内部服务之间的最终一致性。

## 13. 最大努力通知方案

### 13.1 思想

最大努力通知是通知型最终一致性方案。它不保证一定通知成功，而是通过多次通知、重试、查询和人工介入，尽最大努力让接收方知道业务结果。

典型场景：

- 支付平台通知商户支付结果。
- 充值平台通知运营商。
- 第三方回调通知。
- 短信、邮件、物流状态通知。

### 13.2 基本流程

```text
业务主动方完成本地事务
  -> 记录通知任务
  -> 向业务被动方发送通知
  -> 被动方返回成功，任务完成
  -> 被动方失败或超时，主动方按策略重试
  -> 超过最大次数，进入人工处理或等待被动方主动查询
```

### 13.3 支付回调示例

```text
1. 用户在支付平台付款成功。
2. 支付平台记录支付单状态为 SUCCESS。
3. 支付平台通知商户系统。
4. 商户系统收到通知后更新订单为已支付。
5. 商户系统返回 success。
6. 如果商户没有返回 success，支付平台按规则继续通知。
```

重试策略可以是：

```text
15 秒后重试
1 分钟后重试
5 分钟后重试
30 分钟后重试
2 小时后重试
6 小时后重试
24 小时后停止自动通知
```

### 13.4 接收方必须提供查询接口

最大努力通知中，接收方不能完全依赖通知。它还应该能主动查询业务结果。

```text
商户系统没有收到支付通知
  -> 调用支付平台订单查询接口
  -> 查询支付单状态
  -> 如果支付成功，更新本地订单
```

这就是“通知 + 查询 + 对账”的组合。

### 13.5 最大努力通知和本地事件表的区别

| 对比项 | 本地事件表 | 最大努力通知 |
| --- | --- | --- |
| 关注点 | 内部系统之间可靠投递消息 | 跨系统、跨平台结果通知 |
| 一致性责任 | 发送方保证消息可靠进入 MQ，消费方幂等消费 | 发送方尽力通知，接收方也要主动查询 |
| 可靠性 | 更强调消息不丢 | 允许通知失败，但提供查询和对账兜底 |
| 场景 | 内部微服务 | 第三方支付、外部回调 |
| 失败处理 | 重试、死信、补偿 | 阶梯重试、查询、对账、人工介入 |

### 13.6 优点

- 实现相对简单。
- 对主业务链路影响小。
- 适合外部系统不可控的场景。

### 13.7 缺点

- 不能保证强一致。
- 通知可能最终失败。
- 接收方必须做幂等、查询、对账。
- 需要完整监控和人工处理流程。

## 14. 组合存储场景：MySQL 和 Redis 一致性

课程目标中提到 Redis、MySQL 等组件的数据一致性问题。严格说，MySQL 和 Redis 的双写一致性通常不建议使用 XA，而是根据业务特点设计最终一致方案。

### 14.1 常见错误

#### 先更新缓存，再更新数据库

```text
更新 Redis 成功。
更新 MySQL 失败。
结果：缓存是新值，数据库是旧值。
```

#### 先更新数据库，再更新缓存

```text
更新 MySQL 成功。
更新 Redis 失败。
结果：数据库是新值，缓存是旧值。
```

#### 并发写导致旧值覆盖新值

```text
请求 A 更新数据库为 100。
请求 B 更新数据库为 200。
请求 B 先更新缓存为 200。
请求 A 后更新缓存为 100。
结果：数据库 200，缓存 100。
```

### 14.2 常用方案：先更新数据库，再删除缓存

```text
1. 更新数据库。
2. 删除缓存。
3. 下次读请求缓存未命中，从数据库加载新值。
```

为什么删除缓存而不是更新缓存：

- 删除比更新更简单。
- 避免并发更新缓存时旧值覆盖新值。
- 缓存可以按需重建。

### 14.3 删除缓存失败怎么办

可以结合本地事件表或消息队列：

```text
本地事务：
  更新 MySQL。
  写入缓存删除事件。

后台任务：
  扫描事件表。
  删除 Redis key。
  成功后标记事件完成。
  失败后重试。
```

也可以使用 Canal 订阅 MySQL binlog：

```text
MySQL 数据变更
  -> Canal 解析 binlog
  -> 投递缓存失效消息
  -> 缓存服务删除 Redis key
```

### 14.4 延迟双删

某些高并发场景下，会使用延迟双删降低脏缓存概率：

```text
1. 删除缓存。
2. 更新数据库。
3. 延迟一小段时间。
4. 再删除缓存。
```

但延迟时间难以精确设置，工程上仍建议配合消息重试、binlog 订阅、过期时间和对账。

## 15. 方案选型

### 15.1 选型总表

| 方案 | 一致性 | 性能 | 业务侵入 | 复杂度 | 适合场景 |
| --- | --- | --- | --- | --- | --- |
| XA/2PC | 强一致 | 低 | 低 | 中高 | 金融核心、低并发强一致 |
| 3PC | 强一致倾向 | 较低 | 低 | 高 | 理论改进，实际较少直接使用 |
| TCC | 最终一致 | 高 | 高 | 高 | 资源预留、交易链路 |
| 本地事件表 | 最终一致 | 高 | 中 | 中 | 内部微服务异步一致 |
| 最大努力通知 | 弱最终一致 | 很高 | 低中 | 中 | 外部系统通知、支付回调 |
| Saga | 最终一致 | 高 | 高 | 中高 | 长流程、多步骤业务编排 |

### 15.2 按业务问题选择

#### 业务必须同一时刻一致

优先考虑：

- XA/2PC。
- 数据库单库事务。
- 避免拆分事务边界。

注意：如果不是绝对必要，不要轻易把高并发链路做成强一致分布式事务。

#### 业务可以短暂不一致，但最终必须一致

优先考虑：

- 本地事件表。
- 可靠消息。
- TCC。
- Saga。

#### 业务涉及资源预留

优先考虑 TCC。

例如：

- 冻结库存。
- 冻结余额。
- 锁定优惠券。

#### 业务涉及第三方回调

优先考虑最大努力通知。

例如：

- 支付结果通知。
- 物流状态同步。
- 充值结果通知。

#### 业务是内部状态同步

优先考虑本地事件表或 MQ 最终一致。

例如：

- 订单成功后发积分。
- 用户注册后初始化账户。
- 订单状态变更后通知搜索、推荐、风控系统。

### 15.3 面试回答套路

回答分布式事务不要只背方案名，可以按以下结构回答：

```text
1. 先判断业务一致性要求。
   是强一致，还是允许最终一致？

2. 再判断参与方是否可控。
   都是内部系统，还是包含第三方？

3. 再判断业务是否可补偿。
   能否冻结、确认、取消？

4. 最后选择方案。
   强一致：XA/2PC。
   资源预留：TCC。
   内部异步一致：本地事件表/可靠消息。
   外部通知：最大努力通知。

5. 补充工程细节。
   幂等、重试、超时、状态机、对账、监控、人工介入。
```

## 16. 落地必备设计

### 16.1 全局事务 ID

每个分布式事务必须有全局唯一 ID：

```text
transaction_id = 业务前缀 + 时间 + 雪花 ID
```

用途：

- 串联日志。
- 保证幂等。
- 查询事务状态。
- 对账和排查问题。

### 16.2 状态机

不要只用布尔字段表示事务状态。应该设计明确的状态机。

订单示例：

```text
INIT -> PENDING_PAY -> PAID -> FINISHED
INIT -> PENDING_PAY -> CANCELED
PAID -> REFUNDING -> REFUNDED
```

TCC 分支示例：

```text
TRYING -> CONFIRMED
TRYING -> CANCELED
```

事件表示例：

```text
NEW -> SENDING -> SENT
NEW -> FAILED -> DEAD
```

### 16.3 幂等

幂等是分布式事务的底座。

常见方式：

- 唯一索引。
- 去重表。
- 业务单号。
- 状态机判断。
- 乐观锁版本号。
- Redis setnx 短期去重。

示例：

```sql
INSERT INTO consumed_message(message_id, consumer_group, created_at)
VALUES (?, ?, NOW());
```

如果唯一索引冲突，说明消息已经消费过，直接返回成功。

### 16.4 重试

重试需要有边界，不能无限立即重试。

建议：

- 使用指数退避或阶梯重试。
- 记录重试次数。
- 记录下一次重试时间。
- 超过阈值进入死信或人工处理。
- 所有重试操作必须幂等。

### 16.5 超时

所有远程调用都要有超时。没有超时就没有故障边界。

超时后不要简单认为失败，应该进入不确定状态处理：

```text
调用超时
  -> 查询对方状态
  -> 查询失败则标记为 UNKNOWN
  -> 后台任务继续确认
  -> 必要时人工介入
```

### 16.6 对账

对账是最终一致方案的兜底手段。

例如支付场景：

```text
每天拉取支付平台账单。
和本地订单表核对。
发现支付成功但本地未成功的订单。
触发补单任务。
```

### 16.7 监控和告警

需要监控：

- 待处理事务数量。
- 失败事务数量。
- 重试次数分布。
- 死信数量。
- 消息积压。
- 超时调用数量。
- 对账差异数量。

没有监控的最终一致，本质上只是“希望最终一致”。

## 17. 常见面试题

### 17.1 什么是分布式事务

分布式事务是指一次业务操作需要跨多个服务、多个数据库或多个资源完成，并且这些操作之间存在整体一致性要求。由于单个本地事务无法覆盖所有资源，所以需要通过协议协调、业务补偿、可靠消息、重试和对账等机制保证数据一致。

### 17.2 2PC 为什么会阻塞

因为参与者在 Prepare 阶段执行了本地事务并锁定资源，但还没有最终提交。它必须等待协调者发送 Commit 或 Rollback。如果协调者宕机或网络异常，参与者不知道最终决议，只能持有资源等待，从而造成阻塞。

### 17.3 2PC 能完全保证一致性吗

不能说在所有故障场景下绝对保证。2PC 能在正常情况下提供强一致提交语义，但在协调者宕机、网络分区、第二阶段部分参与者收到提交指令等情况下，仍可能出现阻塞或不一致风险。工程上需要日志恢复、协调者高可用、状态查询和人工干预。

### 17.4 3PC 比 2PC 好在哪里

3PC 增加了 CanCommit 阶段，把询问和预提交拆开，并引入参与者超时机制，降低 2PC 的同步阻塞问题。但 3PC 仍无法彻底解决网络分区下的一致性问题，所以实际落地并不一定比柔性事务更常用。

### 17.5 TCC 如何防止空回滚和悬挂

空回滚：Cancel 到达时 Try 可能没有执行，需要记录一条 Cancel 状态，直接返回成功。

悬挂：Try 请求迟到时，需要检查是否已有 Cancel 记录。如果已有 Cancel，Try 不能再执行资源预留。

本质是用事务分支记录和状态机约束 Try、Confirm、Cancel 的执行顺序。

### 17.6 本地事件表如何保证消息不丢

业务数据和事件记录在同一个本地事务中提交。只要业务成功，事件一定落库。后台任务扫描事件表发送消息，发送失败可以重试。这样可以避免业务事务成功但消息没有发出的情况。

### 17.7 本地事件表会不会重复发送消息

会。发送方重试、MQ 重投、消费者处理超时都可能导致重复消息。所以消费者必须幂等，常用消费记录表、唯一索引、业务单号和状态机防重。

### 17.8 最大努力通知和可靠消息最终一致有什么区别

可靠消息最终一致更关注内部系统之间的可靠投递，发送方通常要保证消息可靠进入 MQ，消费方幂等消费。

最大努力通知更适合外部系统或第三方回调。发送方按策略尽力通知，接收方还需要主动查询和对账兜底。它不承诺每次通知一定成功。

### 17.9 Redis 和 MySQL 如何保证一致性

常见做法是先更新 MySQL，再删除 Redis 缓存。如果删除失败，可以通过本地事件表、MQ 重试、binlog 订阅、缓存过期时间和对账兜底。不要试图让 Redis 和 MySQL 像单库事务一样强一致，更多是设计最终一致。

## 18. 总结

分布式事务没有统一最优解，只有和业务匹配的方案。

核心判断标准：

1. 业务是否必须强一致。
2. 能否接受短暂不一致。
3. 参与方是否全部可控。
4. 业务是否可以补偿。
5. 是否能做到幂等、重试、对账、监控。

一句话总结各方案：

- XA/2PC：用协议协调多个资源一起提交，强一致但性能差、易阻塞。
- 3PC：在 2PC 基础上增加预提交和超时机制，降低阻塞但不能根治网络分区问题。
- TCC：业务层预留、确认、取消，性能好但业务侵入强。
- 本地事件表：业务数据和事件同事务落库，再异步投递，适合内部最终一致。
- 最大努力通知：尽力通知、失败重试、查询对账，适合外部系统回调。

真正落地时，方案本身只是一半，另一半是工程保障：幂等、状态机、超时、重试、死信、对账、监控和人工处理。
